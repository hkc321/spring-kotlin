package com.example.spring.config.dto

enum class ErrorCode {
    UNKNOWN_ERROR,
    INVALID_PARAMETER,
    ITEM_NOT_EXIST,
    WRONG_PASSWORD,
    PARAMETER_INVALID,
    ACCESS_TOKEN_EXPIRED,
    REFRESH_TOKEN_EXPIRED,
}